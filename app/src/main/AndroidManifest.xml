<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ir.mrdota.touristplace">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/MyAppTheme">
        <activity android:name=".splash" android:configChanges="screenSize|orientation"  >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main_page" android:configChanges="orientation|screenSize" />
        <activity android:name=".datails" android:configChanges="screenSize|orientation"></activity>
    </application>

</manifest>